using System;
namespace ProgOrieObj
{
    /// <summary>
    /// Par définition toutes les classes de cette interface sont abstraites
    /// </summary>
    interface IDernierMessage
    {
        string DernierMessage(string message);
        void DernierRepas();
    }
}